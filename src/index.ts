/**
 * Given two numbers, add both to produce a sum of the numbers.
 *
 * @param num1 `number` the first number
 * @param num2 `number` the second number
 * @returns `number` the sum of the first and second numbers
 */
export const sum = (num1: number, num2: number): number => num1 + num2;
